/**
 * Creates a beautiful animated beam effect that moves across the screen
 * Perfect for adding visual interest to hero sections and transitions
 * 
 * The beams are customizable through props and use smooth CSS animations
 * for optimal performance
 */
export default function AnimationBeam({ 
  children,
  className,
  containerClassName
}: {
  children?: React.ReactNode;
  className?: string;
  containerClassName?: string;
}) {
  // ... rest of component code ...
} 